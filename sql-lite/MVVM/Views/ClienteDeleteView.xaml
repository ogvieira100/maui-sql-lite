<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="sql_lite.MVVM.Views.ClienteDeleteView"
             Title="ClienteDeleteView">
    <VerticalStackLayout>
        <Label 
            Text="CPF"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <Label 
            Text="{Binding CPF}"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <Label 
            Text="Nome"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <Label 
            Text="{Binding Nome}"
            x:Name="lblNome"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <Label 
            Text="Endereço"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <Label 
            Text="{Binding Endereco}"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <Button
            x:Name="btnDelete"
            Text="Deletar?"
            Clicked="btnDelete_Clicked"
            >
        </Button>
    </VerticalStackLayout>
</ContentPage>